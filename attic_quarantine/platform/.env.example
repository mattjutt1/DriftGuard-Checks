# Database configuration
DATABASE_URL=sqlite+aiosqlite:///./driftguard.db

# Optional: Enable SQL query logging
SQL_ECHO=false

# Strict offline mode flags
PROMPTOPS_MODE=stub
DISABLE_NETWORK=1

# Optional: Allow network access (overrides DISABLE_NETWORK)
# ALLOW_NETWORK=1
